{
  "$schema": "https://docs.renovatebot.com/renovate-schema.json",
  "extends": [
    "config:recommended",
    ":disableDependencyDashboard"
    // not have a GitHub issue listing the open updates
  ],
  "lockFileMaintenance": {
    "enabled": true,
    "automerge": false
  },
  "packageRules": [
    {
      "matchUpdateTypes": [
        "minor",
        "patch",
        "patch",
        "pin",
        "pinDigest",
        "digest"
      ],
      "timezone": "Europe/Berlin",
      "schedule": [
        "every weekday before 5:30am and after 6:30am"
      ],
      // branch creation only on weekdays
      "automerge": false,
      "platformAutomerge": false,
      // this would require more repository setup
      // only create PRs if tests fail (or stall)
    }
  ]
}
