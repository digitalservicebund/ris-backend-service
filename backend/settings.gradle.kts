rootProject.name = "ris-backend-service"
System.setProperty("sonar.gradle.skipCompile", "true")

// for local gradle project use (good for debugging)
//include(":exporter")
//project(":exporter").projectDir = file("../../neuris-juris-xml-export")
