spring:
  config:
    import: "optional:kubernetes:"
  cloud:
    kubernetes:
      config:
        fail-fast: true
        name: database-config
        enabled: true
      secrets:
        enabled: true
        fail-fast: true
        paths: /etc/secrets
