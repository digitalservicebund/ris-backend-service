spring:
  config:
    import: "optional:kubernetes:"
  cloud:
    kubernetes:
      config:
        fail-fast: true
        name: database-config
      secrets:
        enabled: true
        fail-fast: true
        paths: /etc/secrets
