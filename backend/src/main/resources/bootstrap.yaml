spring:
  config:
    import: "optional:kubernetes:"
  cloud:
    kubernetes:
      config:
        enabled: true
        fail-fast: true
        name: database-config
      secrets:
        enabled: true
        fail-fast: true
        paths: /etc/secrets
