spring:
  cloud:
    kubernetes:
      config:
        enabled: true
        fail-fast: true
        name: database-config
      secrets:
        enabled: true
        fail-fast: true
        paths: /etc/secrets
