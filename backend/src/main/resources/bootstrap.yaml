spring:
  cloud:
    bootstrap:
      enabled: true
    kubernetes:
      config:
        fail-fast: true
        name: database-config
      secrets:
        enabled: true
        fail-fast: true
        paths: /etc/secrets
