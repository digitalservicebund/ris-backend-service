import {COLOR_THEMES} from "./themes";

export default {
    ...COLOR_THEMES,
    components: {
        VaButton: {
            rounded: false,
            round: false,
            flat: false,
        },
    },
}
